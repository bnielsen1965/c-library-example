# makefile

This example uses a Makefile to build the library and the application.

The Makefile includes the targets and requirements for both the application 
and the library.

Execute the make command to build of the library and application.

> make

The end result should be the helloworld executable.

> ./helloworld

